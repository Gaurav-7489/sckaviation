import Link from 'next/link';
import {notFound} from 'next/navigation';
import {Header,Footer,Reveal,Inquiry} from '../components';
const pages:Record<string,{eyebrow:string,title:string,subtitle:string,body:string,image:string}>={aircraft:{eyebrow:'OE-LSC / THE AIRCRAFT',title:'Presence.\nWithout precedent.',subtitle:'One aircraft. A complete point of view.',body:'The Gulfstream G450 OE-LSC carries the SCK signature through every layer. Matte and glossy exterior finishes introduce a design language that continues throughout the cabin. Discover an aircraft shaped as much by attitude as by aviation.',image:'aircraft'},design:{eyebrow:'THE SCK SIGNATURE',title:'An obsession.\nIn every detail.',subtitle:'Nothing incidental. Everything considered.',body:'From the interplay of matte and gloss to Portoro marble in the galley, coordinated carpet tones and black Christofle cutlery, every detail belongs to one holistic concept. The G450 interior received recognition at The International Yacht & Aviation Awards in May 2024.',image:'detail'},projects:{eyebrow:'PRODUCTIONS / COLLABORATIONS',title:'Within\nthe impossible.',subtitle:'An unmistakable presence. On and off screen.',body:'OE-LSC appears in Mission: Impossible 8. Its cinematic character opens a conversation beyond conventional aviation: international film productions, special projects and selected collaborations. Tell us what you have in mind.',image:'aircraft'}};
export function generateStaticParams(){return [...Object.keys(pages),'inquire'].map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return {title:slug==='inquire'?'Inquire — SCK Aviation':`${pages[slug]?.eyebrow||'SCK Aviation'} — SCK Aviation`}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;if(slug==='inquire')return <><Header/><main id="main" className="inquire-page"><div><p className="eyebrow">YOUR JOURNEY / YOUR VISION</p><h1>Let’s make<br/><span>an entrance.</span></h1><p>Private travel. Special productions.<br/>Something entirely your own.</p><a className="text-link" href="mailto:charter@sckaviation.com">Prefer email? ↗</a></div><Inquiry/></main><Footer/></>;const page=pages[slug];if(!page)notFound();return <><Header/><main id="main"><section className="sub-hero"><img src={`/images/${page.image}.webp`} alt={slug==='design'?'SCK aircraft design detail':'SCK Aviation black Gulfstream G450'} fetchPriority="high"/><div className="hero-shade"/><div><p className="eyebrow">{page.eyebrow}</p><Reveal><h1>{page.title.split('\n').map((t,i)=><span key={t}>{t}{i===0&&<br/>}</span>)}</h1></Reveal></div></section><section className="section editorial"><p className="eyebrow">ATTITUDE WITH ALTITUDE</p><div><h2>{page.subtitle}</h2><p>{page.body}</p>{slug==='aircraft'&&<dl><div><dt>Aircraft</dt><dd>Gulfstream G450</dd></div><div><dt>Registration</dt><dd>OE-LSC</dd></div><div><dt>Design</dt><dd>SCK signature</dd></div></dl>}<Link className="text-link" href="/inquire">{slug==='projects'?'Discuss a project':'Begin your inquiry'} <span>↗</span></Link></div></section></main><Footer/></>}
